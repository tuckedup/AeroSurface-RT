Weights and optimized engines are generated locally and ignored. Train/export with
`python -m aerosurface.cli train` and `python -m aerosurface.cli export`. TensorRT engines depend
on hardware/runtime version. Real-domain adapted weights have a separate binary label contract.
