Generated frames, temporary smoke jobs, real-domain adapted weights and ROS build products live
here and are ignored. Small measured JSON/CSV records are preserved under `benchmarks/`; the
procedural-only contact sheet is copied into `assets/demo.png`. No real dataset images are published.
